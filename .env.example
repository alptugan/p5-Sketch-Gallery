# Copy this file to `.env` and set credentials
# Never commit real secrets; .env is git-ignored

# Basic Auth credentials for admin and API
ADMIN_USER=admin
ADMIN_PASS=secret

# Server port (optional; defaults to 3000)
# PORT=3000
